[project]
name = "rcm-ai-platform-backend"
version = "0.1.0"
description = "RCM AI Platform backend — FastAPI + SQLAlchemy + pgvector"
requires-python = ">=3.11"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py311"
